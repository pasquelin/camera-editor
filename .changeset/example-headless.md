---
"@media-studio/example-headless": patch
---

Exemple exécutable (Node) démontrant la chaîne headless du SDK : projet + commandes
built-in (undo/redo), licence/gating, catalogue de filtres, transport runtime, et
export en arrière-plan avec encodeur natif simulé.
