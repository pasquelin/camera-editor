{
  "name": "@media-studio/example-headless",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Démo headless du SDK Media Studio (exécutable Node) — projet, commandes, undo/redo, licence, catalogue, runtime, export en arrière-plan.",
  "scripts": {
    "start": "tsx src/demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@media-studio/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
