---
"@media-studio/renderer-preview": minor
---

Nouveau package `@media-studio/renderer-preview` : `<PreviewRenderer>` temps réel
(Skia) composant les calques visuels du projet (images + overlays texte), à brancher
dans le slot `preview` de `<MediaStudio>`. Frames vidéo (lecteur natif), filtres/
stickers GPU = extensions. Conforme à docs/04-RENDERER.md, ADR-0010.
