/**
 * @media-studio/renderer-preview — PreviewRenderer temps réel (Skia). Compose les
 * calques visuels du projet ; à brancher dans le slot `preview` de `<MediaStudio>`.
 * Voir docs/04-RENDERER.md.
 */
export { PreviewRenderer, type PreviewRendererProps } from "./preview-renderer";
