# Site de documentation (Docusaurus)

Documentation **publique** du SDK (distincte du blueprint interne `docs/`).

```bash
cd website && pnpm install && pnpm start   # http://localhost:3000
pnpm build                                  # site statique
```

L'`api-reference/` est générée depuis les commentaires TSDoc du code (docs/25).
